import asyncHandler from "express-async-handler";

/**
 * @desc Store new fields
 * @route POST /api/user-input-fields
 * @access private
 */
export const createUserInputField = asyncHandler(async (req, res) => {
    
});
